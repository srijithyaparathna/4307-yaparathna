pipeline {
    agent any
    
    stages {
        stage('SCM Checkout') {
            steps {
                retry(3) {
                    git branch: 'main', url: 'https://github.com/srijithyaparathna/4307-yaparathna'
                }
            }
        }
        stage('Build Docker Image') {
            steps {
                sh 'docker build -t 4307-yaparathna/nodeapp-cuban:${BUILD_NUMBER} .'
            }
        }
        stage('Login to Docker Hub') {
            steps {
                withCredentials([string(credentialsId: 'samin-docker', variable: 'samindocker')]) {
                    sh "docker login -u adomicarts -p '${samindocker}'"
                }
            }
        }
        stage('Push Image') {
            steps {
                sh 'docker push adomicarts/nodeapp-cuban:${BUILD_NUMBER}'
            }
        }
    }
    post {
        always {
            sh 'docker logout'
        }
    }
}
